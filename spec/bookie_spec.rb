# frozen_string_literal: true

RSpec.describe Bookie do
  it "has a version number" do
    expect(Bookie::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
